name: Deploy to Kubernetes

on:
  workflow_run:
    workflows: ["Build and Push Docker Image"]
    types: [completed]

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up kubectl
        uses: azure/setup-kubectl@v4

      # TODO (Week 3-4): configure kubeconfig for your cluster (minikube for
      # local dev, or an EKS kubeconfig secret for the final deploy) and then
      # apply the manifests, e.g.:
      # - run: kubectl apply -f k8s/deployment-template.yaml
      - name: Placeholder deploy step
        run: echo "Deploy step to be implemented in Week 3-4"

