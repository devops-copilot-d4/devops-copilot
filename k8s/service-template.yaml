apiVersion: v1
kind: Service
metadata:
  name: "{{DEPLOYMENT_NAME}}-svc"
  namespace: "{{NAMESPACE}}"
spec:
  selector:
    app: "{{DEPLOYMENT_NAME}}"
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP

