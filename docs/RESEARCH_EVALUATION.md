# RESEARCH EVALUATION & EXPERIMENTAL METHODOLOGY

**Project Title:** AI DevOps Copilot – AI Agent for Autonomous CI/CD Failure Prediction and Self-Healing  
**Institution:** The National Institute of Engineering, Department of Computer Science & Engineering  

---

## 1. Central Research Question

> **“Can an AI agent analyze CI/CD and Kubernetes operational data, predict deployment failures, determine an appropriate recovery action, autonomously execute that recovery, and verify whether the recovery was successful?”**

---

## 2. Controlled Failure Classes Matrix

| # | Failure Scenario | Detection Signals | Root Cause Category | Pre-approved Recovery Action |
|---|---|---|---|---|
| 1 | **CrashLoopBackOff** | Repeated exits ($N \ge 3$), `exit code 1`, non-zero error rate | Startup crash / DB connection failure | `ROLLBACK` |
| 2 | **OOMKilled** | Memory $\ge 90\%$, `OOMKilled` event, exit code 137 | Cgroup memory limit exhaustion | `SCALE` / Resource Adjustment |
| 3 | **High CPU** | CPU utilization $\ge 85\%$, latency $> 2.0\text{s}$ | Compute loop saturation | `SCALE` (HPA replica increase) |
| 4 | **Failed Deployment** | `ImagePullBackOff`, rollout timeout $> 90\text{s}$ | Registry pull failure / invalid tag | `ROLLBACK` |
| 5 | **Application Health Failure** | Liveness probe `HTTP 500`, high error rate | Deadlock / connection pool exhaustion | `RESTART` (Rolling restart) |
| 6 | **Configuration Error** | `CreateContainerConfigError`, missing secret | Undefined environment variable | `ROLLBACK` |

---

## 3. ML Model Performance Metrics

**Status: preliminary, from synthetic data — not yet valid for final
submission. See `docs/ML_FINDINGS_AND_FIXES.md` for a known issue.**

Evaluated on 2,500 synthetic controlled telemetry instances (generated by
`ml/generate_dataset.py`, seed=42) using an 80/20 train/test split with
5-fold cross-validation, actually run via `ml/train.py` on 2026-09-03:

| Metric | Random Forest (Binary Failure) | Random Forest (Multi-Class) |
|---|---|---|
| **Cross-val F1 (5-fold)** | 1.0000 (+/- 0.0000) | — |
| **Accuracy** | 1.0000 | 1.0000 |
| **Precision** | 1.0000 | — |
| **Recall** | 1.0000 | — |
| **F1-Score** | 1.0000 | 1.0000 (weighted) |

**These numbers are not presentable as-is.** A perfect 1.0000 across
every metric is a strong signal of data leakage, not genuine predictive
skill — the categorical fields `pod_status`, `deployment_status`, and
`health_status` currently encode the failure outcome directly, so the
model is classifying an already-visible failure rather than predicting
one before it occurs. See `docs/ML_FINDINGS_AND_FIXES.md` for the root
cause and the fix required before these figures can be used in the final
report or viva. Once the feature set is corrected to use only precursor
signals and the dataset regenerated, expect (and prefer) a lower,
genuinely defensible accuracy in roughly the 80-95% range.

All data in this section is **synthetic**, generated by a rule-based
script, not collected from real controlled cluster experiments. Per the
project brief, final evaluation should additionally include results from
real fault-injection runs against the actual Minikube/Kind cluster
(see `scripts/inject_fault.js`) before submission.

---

## 4. System-Level Comparative Evaluation

**Status: not yet measured — placeholder structure only.**

The table below is the intended comparison structure between the
**Traditional Manual DevOps Incident Response** and the **AI DevOps
Copilot Autonomous Flow**. The previous version of this document filled
in specific numbers here (e.g. "4.2-8.5 seconds MTTD") without having run
any real timed experiment. Per the project brief's explicit instruction
- do not invent numbers, generate results only after experiments - those
figures have been removed.

To fill this in for real:
1. Use `scripts/inject_fault.js` (or manual `kubectl` commands) to
   trigger each of the 6 controlled failure classes against the running
   cluster, multiple times each, for statistical validity.
2. Time from fault injection to (a) the dashboard/Incident record showing
   detection, and (b) the RecoveryAction record showing
   `requirementVerified: true` — these are your real MTTD and MTTR.
3. Record how many runs resolved without human approval vs. how many hit
   the approval gate or failed, for the recovery success rate row.
4. For the "traditional manual workflow" baseline, either time a team
   member manually diagnosing and fixing the same injected fault without
   using the AI Copilot, or cite it clearly as an estimated baseline
   (not a measured one) if a real manual trial isn't feasible in the
   time available.

| Evaluation Metric | Traditional Manual Workflow | AI DevOps Copilot Workflow | Improvement (%) |
|---|---|---|---|
| **Mean Time to Detect (MTTD)** | *(to be measured/estimated)* | *(to be measured — real timed run)* | *(computed from measured values)* |
| **Mean Time to Recover (MTTR)** | *(to be measured/estimated)* | *(to be measured — real timed run)* | *(computed from measured values)* |
| **Autonomous Recovery Success Rate** | 0% (fully manual, by definition) | *(measured: successful / total injected runs)* | — |
| **Manual Intervention Touchpoints** | *(count real manual steps)* | *(count from actual approval-gate logs)* | *(computed)* |
| **Safety Violations / Cascade Loops**| *(not applicable to manual)* | *(count from actual test runs — should be 0 given the allow-list/cooldown guards)* | — |

---

## 5. Research Contribution Framing

This research contributes a **practical, hybrid, closed-loop AI DevOps platform** that bridges the gap between predictive statistical machine learning (Random Forest), contextual reasoning (compact log LLM agent), and deterministic safety-enforced execution in container orchestration. The primary novelty lies in the **integrated automated workflow and experimental verification** — this is not claimed to be the first system to use any individual technique (ML failure prediction, LLM log analysis, or Kubernetes self-healing each have existing prior work), but rather an integrated pipeline combining them with an explicit safety-guarded execution layer and post-recovery verification. Demonstrable improvements in cloud-native operational reliability are pending the real experimental results described in Sections 3 and 4 above.
